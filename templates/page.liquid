<div class="wrap" style="padding:100px 40px">
  <h1 style="font-size:clamp(40px,5vw,80px);margin-bottom:40px">{{ page.title }}</h1>
  <div class="page-content" style="max-width:72ch;font-size:17px;line-height:1.65;color:var(--ink-2)">
    {{ page.content }}
  </div>
</div>
